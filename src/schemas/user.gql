mutation Login($input: LoginInput!) {
  login(input: $input) {
    firstName
    lastName
    email
    type
    token
  }
}

mutation Register($input: RegistrationInput!) {
  register(input: $input) {
    firstName
    lastName
    email
    type
    token
  }
}